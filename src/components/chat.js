import React from 'react';
import { connect } from 'react-redux';

const Chat = (props) => {
    return (
      <div>
        
      </div>
    );
  };
  
  export default connect(null, { })(Chat);

  