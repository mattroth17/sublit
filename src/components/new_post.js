import React from 'react';
import { connect } from 'react-redux';

const NewPost = (props) => {
    return (
      <div>
        
      </div>
    );
  };
  
export default connect(null, { })(NewPost);
